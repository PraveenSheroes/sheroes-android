<network-security-config>
   <debug-overrides> <!-- Only trust user certificates in Debug mode -->
      <trust-anchors>
         <certificates src="system" />
         <certificates src="user" />
      </trust-anchors>
   </debug-overrides>
</network-security-config>
